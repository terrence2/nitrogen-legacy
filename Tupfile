.gitignore
CFLAGS += -Wall -std=c++14 -Ideps/glm @(CFLAGS)
CFLAGS += `pkg-config --cflags glfw3`

LIBS += `pkg-config --libs glfw3` -lGLESv2

: foreach src/*.cpp |> @(CC) $(CFLAGS) -c %f -o %o |> %B.o
: *.o |> @(CC) $(CFLAGS) %f -o %o $(LIBS) |> fsim@(EXT)
